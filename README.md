Generate with the following:

```
$ git clone https://github.com/yati-sagade/blog
$ cd blog
$ python -mvenv ~/.virtualenvs/blog
$ . ~/.virtualenvs/blog/bin/activate
(blog) $ pip install --upgrade -r requirements.txt
(blog) $ python generate.py --site-directory=./site --copy-static=./static/images .
```


